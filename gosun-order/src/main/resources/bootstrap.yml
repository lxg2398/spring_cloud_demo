spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      profile: dev
  application:
    name: gosun-order
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/